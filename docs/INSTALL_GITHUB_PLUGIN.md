# GitHub Plugin (Tool) — not a Skill

1. Workspace → Tools (or Admin → Functions)
2. Create / Import → paste `plugins/github_tool.py`
3. Enable tool
4. Valves → GITHUB_TOKEN = PAT
5. In chat enable the tool icon, then ask to list repos

Never paste tokens in chat.
